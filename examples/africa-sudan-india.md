# 수단에서 인도로

아래는 GeoInsight 최소형 펜스다. 역할 추론만으로 첨부 레퍼런스 이미지를 재현한다 —
아프리카는 대륙이므로 `group`(amber, 내부 국경 유지), 수단·인도는 `link` 끝점이므로
`focus`(coral). 수단 국경에서 인도로 taper 화살표가 출발한다.

```geoinsight
earth:
  show: 아프리카, 수단, 인도
  link: 수단 -> 인도
```

이어지는 설명…
